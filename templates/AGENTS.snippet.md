## Engineering skills

This repo uses the swe-skills library. Start with the `using-swe-skills` skill to choose the 1–3 skills relevant to the task and to size the process (tool / app / system).

- Verify before claiming: run the gate commands below and show their output.
- Ask before push, tag, publish, delete, or writes outside this repo.

### Gate commands
<!-- replace with this project's exact commands -->
```bash
# test:   <command>
# lint:   <command>
# docs:   <command>
```
